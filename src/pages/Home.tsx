import React from 'react'
import { RoutesWrapper } from '../components'

const Home = (): React.ReactElement => {
  return (
    <RoutesWrapper>
      <div>Home Page</div>
    </RoutesWrapper>
  )
}

export default Home
